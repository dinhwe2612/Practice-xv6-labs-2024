Looking at the backtrace output, which function called syscall?
-> syscall is called by usertrap at kernel/trap.c.
What is the value of p->trapframe->a7 and what does that value represent?
-> the value of p->trapframe->a7 is 7 and that number is used to index into syscalls, which is SYS_exec based on user/initcode.S.
What was the previous mode that the CPU was in?
-> User mode because in trap.c, it has a condition check to if the previous is user mode or not.
Write down the assembly instruction the kernel is panicing at. Which register corresponds to the variable num?
-> 0x80001c92 <syscall+16> lw   a3,0(zero) # 0x0 and the corresponding register is a3.
Why does the kernel crash? Hint: look at figure 3-3 in the text; is address 0 mapped in the kernel address space? Is that confirmed by the value in scause above? (See description of scause in RISC-V privileged instructions)
-> The kernel crash when it tries to read the value of the address 0x0 and scause is 0xd which corresponds to Load page fault based on the instructions.
What is the name of the binary that was running when the kernel paniced? What is its process id (pid)?
-> the name of the binary is initcode and its process id is 1.